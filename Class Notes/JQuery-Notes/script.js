const form = $("form");

console.log(form);

console.log(firstNameInput.value);

$('button#submit-btn').click(function(event){
    event.preventDefault(); 
    // console.log($("form"));
    console.log($(''));
    console.log($("form")[0].elements);

    const myFormArray = $("form")[0].elements;
    for (letter input myFormArray){

    }
})